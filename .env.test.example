# Integration Test Environment Variables
# Copy this file to .env.test and fill in your test database credentials

# Supabase Configuration (Required for all tests)
VITE_SUPABASE_URL=your_supabase_project_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Service Role Key (Required for integration tests with database operations)
# WARNING: This key bypasses Row Level Security - only use in test environments!
# DO NOT commit this key to git or use it in production!
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Optional: Better Auth URL for auth tests
VITE_BETTER_AUTH_URL=http://localhost:5173/api/auth

# Node Environment
NODE_ENV=test

# Test Database Configuration (if using separate test database)
# DATABASE_URL=postgresql://user:password@localhost:5432/pythoughts_test

# Notes:
# 1. Create a separate Supabase project for testing or use a test database
# 2. Run migrations on your test database before running integration tests
# 3. The service role key is required for creating/cleaning up test data
# 4. Never commit .env.test to git - it's in .gitignore for security
